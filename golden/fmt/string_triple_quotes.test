(
  // This is a regression test, should not escape spaces before the "" even
  // though the lexer breaks the token up at that point.
  """
  "" <- That breaks up this line for the lexer."""
)

# output:
(
  // This is a regression test, should not escape spaces before the "" even
  // though the lexer breaks the token up at that point.
  """
  "" <- That breaks up this line for the lexer."""
)
